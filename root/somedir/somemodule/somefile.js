/***
 * Problem Statement
 * Challenge Write an application/script that when executed in a directory, 
 * produces a list of all files (using their absolute paths) containing the keyword "toDog" in them. 
 * The files can be in the immediate directory, or a sub-directory (or a sub-directory of the 
 * sub-directory, ad infinitum).
 ***/